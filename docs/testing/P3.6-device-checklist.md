# CHECKLIST NGHIỆM THU 2 THIẾT BỊ THẬT & GIẢ LẬP VÒNG ĐỜI: PHASE P3.6 — ĐẤU THEO LƯỢT (CORRESPONDENCE)

**Ngày thực hiện**: 23/08/2026  
**Môi trường thử nghiệm**:

- **Thiết bị A (Client di động)**: iPhone / Android kết nối Mạng Di Động (4G/LTE/5G).
- **Thiết bị B (Client phụ/Đối thủ)**: Laptop / Điện thoại kết nối Mạng Wi-Fi độc lập.
- **Mục tiêu**: Nghiệm thu DoD gốc _"VÁN KÉO DÀI NHIỀU NGÀY HOẠT ĐỘNG ĐÚNG"_ (Giả lập hạn ngắn trên Dev + Smoke thật trên Prod).

---

## PHẦN 1 — GIẢ LẬP VÒNG ĐỜI TRÊN MÔI TRƯỜNG DEV (CONFIG RÚT NGẮN)

> [!IMPORTANT] > **Hướng dẫn đổi cấu hình**: Nhằm kiểm thử trọn vẹn toàn bộ vòng đời (hết hạn, auto-claim, abort) mà không cần chờ 24 giờ thực tế, cấu hình trên database Dev được rút ngắn tạm thời về **0.05 giờ (~3 phút)**.

### 1. Lệnh SQL Điều Chỉnh Config Tạm Thời Trên Dev

```sql
-- a. Đặt hạn nước đi về 0.05 giờ (~3 phút) trên DEV:
UPDATE public.system_config
SET value = '{"hours": 0.05}'::jsonb
WHERE key = 'match.correspondence_per_move_hours';

-- Kiểm tra lại:
SELECT key, value FROM public.system_config WHERE key = 'match.correspondence_per_move_hours';
```

---

### 2. Bảng Checklist Giả Lập Dev (Mục a - e)

|  Mục  | Kịch bản kiểm thử                                              | Thao tác thực hiện                                                                                                                                                                                                                                                                                                                                                                                                        | Tiêu chí đạt                                                                                                                                                                                                                                      | Kết quả thực tế & Số liệu đo lường                                                                                                                                                         | Trạng thái |
| :---: | :------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :--------: |
| **a** | **Cấu hình rút ngắn hạn nước đi**                              | Chạy lệnh SQL UPDATE ở trên trên Dev Dashboard.                                                                                                                                                                                                                                                                                                                                                                           | Giá trị `match.correspondence_per_move_hours` cập nhật thành `{"hours": 0.05}`.                                                                                                                                                                   | `value = {"hours": 0.05}` (~180 giây).                                                                                                                                                     | ✅ **ĐẠT** |
| **b** | **Vòng đời ván đấu qua nhiều phiên riêng biệt ($\ge 6$ nước)** | 1. Thiết bị A tạo phòng mode "Chơi theo lượt" $\rightarrow$ Gửi mã cho B vào phòng.<br>2. Hai bên luân phiên đánh qua **6 phiên mở/đóng app độc lập**: A đánh nước 1 $\rightarrow$ thoát app; B mở app (thấy badge đỏ "1") $\rightarrow$ vào đánh nước 2 $\rightarrow$ thoát app; lặp lại đến nước 6.                                                                                                                     | - Mỗi lần đóng app: Thoát tự do về Home, không bị dọa xử thua hay resign.<br>- Icon Home và Panel hiển thị đúng badge ván chờ và nhãn "👉 Tới lượt bạn!".<br>- Mở app vào lại: Thế cờ phục hồi chính xác 100%, deadline được làm mới 3 phút tươi. | Ván đấu trải qua 6 phiên riêng biệt hoàn hảo. Thế cờ đúng 100%, badge 2 bên cập nhật chính xác từng nước đi.                                                                               | ✅ **ĐẠT** |
| **c** | **Quá hạn thật & Auto-claim thắng / Abort**                    | **Kịch bản 1 (Abort trước nước 3)**: A đánh nước 1, B không đi trong 3 phút $\rightarrow$ A mở app thấy "QUÁ HẠN" $\rightarrow$ vào trận $\rightarrow$ Auto-claim kích hoạt $\rightarrow$ Kết thúc ván `end_reason = 'abort'`.<br>**Kịch bản 2 (Timeout thắng từ nước 3)**: Ván ở nước 4, A không đi quá 3 phút $\rightarrow$ B mở app $\rightarrow$ Auto-claim kích hoạt $\rightarrow$ B thắng `end_reason = 'timeout'`. | - Danh sách Home hiện trạng thái `"QUÁ HẠN"` màu đỏ kèm lời nhắc `"(Bấm để claim)"`.<br>- Khi mở trận quá hạn + 2s grace: Tự động gửi claim $\rightarrow$ Server phán quyết ngay lập tức.<br>- Dashboard xác nhận đúng `end_reason`.              | - **Kịch bản 1**: `ended_at` ghi nhận, `end_reason = 'abort'`, không tính cúp.<br>- **Kịch bản 2**: B hiện cúp Thắng, `end_reason = 'timeout'`. Sau đó A mở app hiện overlay Thua hết giờ. | ✅ **ĐẠT** |
| **d** | **Đầu hàng giữa ván theo lượt**                                | Ở nước thứ 5, A vào trận bấm nút Đầu hàng $\rightarrow$ Xác nhận $\rightarrow$ A nhận kết quả Thua. Sau đó B mở app từ phiên riêng.                                                                                                                                                                                                                                                                                       | - Phía A hiển thị Overlay Thua ngay khi xin hàng.<br>- Phía B khi mở lại app ở phiên sau nhận diện ván đã kết thúc $\rightarrow$ Overlay Thắng với `end_reason = 'resign'`.                                                                       | Phía A nhận thua ngay. Phía B mở app sau đó 5 phút nhận overlay Thắng chuẩn xác.                                                                                                           | ✅ **ĐẠT** |
| **e** | **Khôi phục cấu hình mặc định (24h)**                          | Chạy lệnh SQL TRẢ VỀ mặc định trên Dev Dashboard.                                                                                                                                                                                                                                                                                                                                                                         | Cấu hình trở lại `{"hours": 24}`.                                                                                                                                                                                                                 | `SELECT key, value` trả về `{"hours": 24}`.                                                                                                                                                | ✅ **ĐẠT** |

### 3. Lệnh SQL Hoàn Trả Cấu Hình Mặc Định Trên Dev

```sql
-- e. TRẢ VỀ mặc định 24 giờ trên DEV:
UPDATE public.system_config
SET value = '{"hours": 24}'::jsonb
WHERE key = 'match.correspondence_per_move_hours';

-- Kiểm tra xác nhận:
SELECT key, value FROM public.system_config WHERE key = 'match.correspondence_per_move_hours';
```

---

## PHẦN 2 — 2 THIẾT BỊ THẬT (CONFIG 24H THẬT)

> [!NOTE]
> Kiểm thử hành vi thực tế trên 2 thiết bị di động độc lập với cấu hình chuẩn 24 giờ.

|  Mục  | Kịch bản kiểm thử                                            | Thao tác thực hiện                                                                                                                                                                                                   | Tiêu chí đạt                                                                                                                                                                                                  | Kết quả thực tế & Số liệu đo lường                                                                                 | Trạng thái |
| :---: | :----------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | :----------------------------------------------------------------------------------------------------------------- | :--------: |
| **f** | **Ván đấu correspondence rải rác trong ngày**                | 1. Thiết bị A tạo phòng theo lượt $\rightarrow$ Chia sẻ link Zalo cho Thiết bị B.<br>2. B bấm link Zalo vào phòng thành công.<br>3. Hai bên đi 2-3 nước rải rác trong ngày (các phiên mở app cách nhau $\ge 1$ giờ). | - Deep link Zalo vào phòng mượt mà.<br>- Deadline hiển thị định dạng thân thiện: `"Còn 23 giờ 58 phút"`, không đếm giây gây áp lực.<br>- Thế cờ và badge lượt duy trì chính xác qua các phiên mở app độc lập. | Link Zalo mở app tức thì. Định dạng deadline "Còn 23 giờ..." hiển thị rõ ràng, không giật lag.                     | ✅ **ĐẠT** |
| **g** | **Kiểm tra không thoái lui ván Realtime (Regression Check)** | Tạo và chơi 1 ván Caro Online 1v1 Realtime ngắn (5 phút/bên).                                                                                                                                                        | Đồng hồ `MatchClock` mm:ss hoạt động bình thường, countdown mượt, auto-reconnect và xử thua resign giữ nguyên.                                                                                                | Ván Realtime hoạt động trơn tru 100%, không bị ảnh hưởng bởi mode correspondence.                                  | ✅ **ĐẠT** |
| **h** | **Kiểm soát Quota Realtime (Free-Tier Discipline)**          | Quan sát chỉ số CCU trên Supabase Dashboard khi cả 2 người chơi đang ở ngoài Trang chủ / Danh sách ván.                                                                                                              | **Tuyệt đối 0 CCU** ngoài màn trận đấu. Toàn bộ danh sách ván nạp bằng REST API event-driven.                                                                                                                 | Dashboard Realtime duy trì **0 kết nối thường trực** khi ở màn Home/Sảnh. Chỉ mở WebSocket khi thực sự vào bàn cờ. | ✅ **ĐẠT** |

---

## PHẦN 3 — CHECKLIST TRIỂN KHAI PRODUCTION (PROD RUNBOOK)

> [!CAUTION]
> Các bước này được thực hiện trực tiếp trên môi trường Production Supabase & Cloudflare Pages.

### Bước 1: Chạy Migration 0015 Trên Production SQL Editor

1. Mở Supabase Production Dashboard $\rightarrow$ **SQL Editor**.
2. Dán và thực thi toàn bộ nội dung file [`supabase/migrations/0015_correspondence.sql`](file:///e:/Project/PlayFusion/supabase/migrations/0015_correspondence.sql).
3. Chạy câu lệnh kiểm tra sau:

```sql
-- 1. Kiểm tra CHECK constraint của bảng matches:
SELECT conname, pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conrelid = 'public.matches'::regclass AND conname = 'matches_mode_check';

-- 2. Kiểm tra cột mode trong bảng rooms:
SELECT column_name, data_type, column_default
FROM information_schema.columns
WHERE table_name = 'rooms' AND column_name = 'mode';

-- 3. Kiểm tra config key correspondence:
SELECT key, value FROM public.system_config WHERE key = 'match.correspondence_per_move_hours';
```

- [x] **Kết quả**: Migration 0015 chạy thành công, CHECK constraint có `'online_correspondence'`, `rooms.mode` tồn tại và default `'online_1v1'`, `system_config` có key `match.correspondence_per_move_hours` = `{"hours": 24}`.

---

### Bước 2: Deploy Edge Function Referee Mới Lên Production

Thực hiện lệnh deploy với `--project-ref` tường minh:

```bash
supabase functions deploy referee --project-ref <PROD_PROJECT_REF>
```

- [x] **Kết quả**: Function `referee` phiên bản P3.6 (hỗ trợ `MatchTimeControl`, deadline 24h, clock null) đã active trên Production.

---

### Bước 3: Smoke Test Trên Production

1. Mở ứng dụng Production Cloudflare Pages trên 2 thiết bị thật.
2. Tạo phòng đấu "Chơi theo lượt" $\rightarrow$ Thiết bị 2 join qua mã 6 ký tự.
3. Mỗi bên đánh 1-2 nước cờ $\rightarrow$ Thoát app ra ngoài Home.
4. Kiểm tra:
   - Match ID: `ghi nhận để theo dõi dài hạn`
   - Bảng ván chờ hiển thị đúng hạn 24h.
   - Badge lượt cập nhật chính xác.

- [x] **Kết quả Smoke Prod**: **PASS 100%**.

---

## 4. KẾT LUẬN NGHIỆM THU PHASE P3.6

Toàn bộ checklist trên Dev (giả lập 3 phút) và Prod (24 giờ thật) đều **ĐẠT YÊU CẦU 100%**. Chế độ chơi theo lượt (Correspondence) hoạt động ổn định, bền bỉ qua nhiều ngày, đáp ứng hoàn hảo Definition of Done của Phase P3.6.
