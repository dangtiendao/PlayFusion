# MA TRẬN PHÂN QUYỀN TRUY CẬP CƠ SỞ DỮ LIỆU (RLS ACCESS MATRIX)

> [!IMPORTANT] > **QUY TẮC BẤT BIẾN**: Tài liệu này là nguồn chân lý duy nhất (Single Source of Truth) về chính sách phân quyền Row Level Security (RLS) của toàn bộ dự án Web Game Hub (PlayFusion). Mọi tính năng hoặc migration mới (P4.2 `settle_match`, P5.1 `admin_dashboard`, P6.2 `purchase`) **BẮT BUỘC phải cập nhật ma trận này TRƯỚC khi viết code SQL**.

---

## 1. QUY ƯỚC KÝ HIỆU & ĐỊNH NGHĨA NGỮ CẢNH

- **Ngữ cảnh người dùng**:
  1. `anon` _(Khách chưa xác thực)_: Người dùng truy cập web mà chưa có phiên đăng nhập Supabase (Lưu ý: PlayFusion luôn tự động khởi tạo Anonymous Auth Session nên `anon` hầu như chỉ xuất hiện khi khách vừa tải trang lần đầu tiên).
  2. `authenticated (Chính chủ)`: Người dùng đã đăng nhập (cả Google và Ẩn danh) đang thao tác trên **dòng dữ liệu của chính mình** (`auth.uid() = user_id`).
  3. `authenticated (Người khác)`: Người dùng đã đăng nhập thao tác trên **dòng dữ liệu của người dùng khác** (`auth.uid() <> user_id`).
  4. `service_role` _(Backend / Admin / Edge Functions)_: Các tiến trình chạy với Supabase Service Key, tự động **Bypass RLS** để thực hiện logic nghiệp vụ server có kiểm soát.
- **Ký hiệu quyền**:
  - ✅ : Cho phép hoàn toàn.
  - ❌ : Cấm tuyệt đối tại tầng Database (`deny by default`).
  - ⚠️ : Cho phép có điều kiện (Ghi rõ điều kiện kiểm tra RLS / Trigger).

---

## 2. MA TRẬN PHÂN QUYỀN TOÀN BỘ 15 BẢNG CƠ SỞ DỮ LIỆU

| Bảng Cơ Sở Dữ Liệu                                            |      Thao tác       | `anon`<br>_(Chưa Auth)_ | `authenticated`<br>_(Chính chủ)_ | `authenticated`<br>_(Người khác)_ | `service_role`<br>_(Backend / Edge)_ | Ghi chú & Kế hoạch mở rộng theo Phase                                                                                                       |
| :------------------------------------------------------------ | :-----------------: | :---------------------: | :------------------------------: | :-------------------------------: | :----------------------------------: | :------------------------------------------------------------------------------------------------------------------------------------------ |
| **1. `public.profiles`**<br>_(Hồ sơ người chơi)_              |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Hồ sơ công khai để xem đối thủ trong trận, phòng chờ và bảng xếp hạng.                                                                      |
|                                                               |     **INSERT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Tự động tạo qua Trigger `handle_new_user` khi đăng ký `auth.users`.                                                                         |
|                                                               |     **UPDATE**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Chỉ được đổi `display_name`, `avatar_url`. Trigger `protect_profile_system_fields` chặn đổi `role`, `user_id`, `is_anonymous`.              |
|                                                               |     **DELETE**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Tự động CASCADE khi xóa user trong `auth.users`. Xóa người dùng chủ động sẽ dùng cơ chế Soft-Delete Profile ở tương lai.                    |
| **2. `public.games`**<br>_(Danh mục game)_                    |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Danh mục game công khai trong toàn ứng dụng (`is_active = true`).                                                                           |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Chỉ cập nhật qua migration hoặc Admin CMS (P5.1).                                                                          |
| **3. `public.seasons`**<br>_(Mùa giải thi đấu)_               |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Thông tin mùa giải thi đấu công khai cho toàn thể người chơi.                                                                               |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Chỉ cập nhật qua migration hoặc Cron chuyển mùa (P5.1).                                                                    |
| **4. `public.matches`**<br>_(Bản ghi ván đấu)_                |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Lịch sử ván đấu công khai để phục vụ xem lại (Replay), tra cứu lịch sử và đối soát rank.                                                    |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client tuyệt đối. Chỉ Edge Function `settle_match` (P4.2) mới được ghi kết quả sau khi đối soát engine.                            |
| **5. `public.match_participants`**<br>_(Đấu thủ trong ván)_   |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Danh sách người chơi tham gia ván đấu công khai để đối chiếu tỷ số.                                                                         |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client tuyệt đối. Chỉ Edge Function `settle_match` (P4.2) mới được ghi bản ghi đấu thủ.                                            |
| **6. `public.player_ratings`**<br>_(Điểm rank & Leaderboard)_ |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Điểm xếp hạng công khai phục vụ hiển thị bảng xếp hạng Leaderboard và hồ sơ cá nhân.                                                        |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client tuyệt đối. Cấm client tự cập nhật rating; chỉ Edge Function `settle_match` (P4.2) mới cập nhật điểm rank và bộ đếm.         |
| **7. `public.wallets`**<br>_(Số dư ví xu)_                    |     **SELECT**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Số dư xu là thông tin tài chính riêng tư (`auth.uid() = user_id`).                                                                          |
|                                                               |     **INSERT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Tự động tạo qua Trigger `handle_new_profile_wallet` khi tạo profile.                                                                        |
|                                                               |     **UPDATE**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client tuyệt đối (`deny-write`). Chỉ Server Edge Function cộng/trừ xu sau khi thắng trận (P4.2) hoặc nạp tiền (P6.2).              |
|                                                               |     **DELETE**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Tự động CASCADE khi xóa profile.                                                                                                            |
| **8. `public.wallet_transactions`**<br>_(Sổ cái tài chính)_   |     **SELECT**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Lịch sử biến động số dư chỉ chính chủ xem được (`auth.uid() = user_id`).                                                                    |
|                                                               |     **INSERT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client tuyệt đối. Chỉ Server Edge Function ghi nhận giao dịch kèm `idempotency_key`.                                               |
|                                                               | **UPDATE / DELETE** |           ❌            |                ❌                |                ❌                 |                  ❌                  | **Bất biến (Append-Only)**: Trigger chặn đứng 100% lệnh UPDATE/DELETE, kể cả service_role. Sửa sai DUY NHẤT bằng giao dịch bù.              |
| **9. `public.shop_items`**<br>_(Danh mục Cửa Hàng)_           |     **SELECT**      |      ⚠️ _(Active)_      |          ⚠️ _(Active)_           |           ⚠️ _(Active)_           |                  ✅                  | Chỉ hiển thị các vật phẩm đang kích hoạt (`is_active = true`) và trong khung giờ mở bán (`available_from <= now() <= available_until`).     |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Quản lý danh mục qua Admin CMS (P5.1).                                                                                     |
| **10. `public.user_inventory`**<br>_(Kho đồ sở hữu)_          |     **SELECT**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Kho đồ cá nhân chỉ chính chủ xem được (`auth.uid() = user_id`).                                                                             |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Chỉ cấp phát qua Trigger ban đầu, Edge Function mua đồ (P6.2), hoặc phần thưởng mùa (P4.2).                                |
| **11. `public.user_equipped`**<br>_(Trang bị đang mặc)_       |     **SELECT**      |           ✅            |                ✅                |                ✅                 |                  ✅                  | Công khai để hiển thị avatar, khung viền, skin bàn cờ/quân cờ trong phòng chờ và ván đấu.                                                   |
|                                                               |     **INSERT**      |           ❌            |        ⚠️ _(WITH CHECK)_         |                ❌                 |                  ✅                  | **Policy ghi DUY NHẤT mở cho client**: Bắt buộc chính chủ VÀ **đã sở hữu vật phẩm** trong `user_inventory`.                                 |
|                                                               |     **UPDATE**      |           ❌            |        ⚠️ _(WITH CHECK)_         |                ❌                 |                  ✅                  | Đổi trang bị slot tức thì qua client, kiểm tra sở hữu tại tầng DB.                                                                          |
|                                                               |     **DELETE**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Tháo trang bị về mặc định.                                                                                                                  |
| **12. `public.purchases`**<br>_(Lịch sử mua sắm)_             |     **SELECT**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Lịch sử mua hàng chỉ chính chủ xem được (`auth.uid() = user_id`).                                                                           |
|                                                               |     **INSERT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Ghi nhận chứng từ qua Edge Function mua hàng (P6.2).                                                                       |
|                                                               | **UPDATE / DELETE** |           ❌            |                ❌                |                ❌                 |                  ❌                  | **Bất biến (Append-Only)**: Trigger chặn đứng 100% lệnh UPDATE/DELETE để bảo toàn chứng từ kế toán.                                         |
| **13. `public.audit_logs`**<br>_(Nhật ký Admin)_              |     **SELECT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa trắng hoàn toàn với mọi client. Sẽ mở policy SELECT có kiểm tra `role = 'admin'` ở Phase P5.1 (Admin Dashboard).                       |
|                                                               |     **INSERT**      |           ❌            |                ❌                |                ❌                 |                  ✅                  | Chỉ ghi nhận nhật ký can thiệp đặc quyền qua Server `service_role`. Cột `reason` bắt buộc $\ge 3$ ký tự.                                    |
|                                                               | **UPDATE / DELETE** |           ❌            |                ❌                |                ❌                 |                  ❌                  | **Bất biến (Append-Only)**: Trigger chặn đứng 100% lệnh UPDATE/DELETE.                                                                      |
| **14. `public.system_config`**<br>_(Cấu hình hệ thống)_       |     **SELECT**      |  ⚠️ _(Key công khai)_   |       ⚠️ _(Key công khai)_       |       ⚠️ _(Key công khai)_        |                  ✅                  | Client chỉ đọc các key có tiền tố công khai (`reward.%`, `match.%`, `penalty.%`, `elo.%`). Các key nhạy cảm (như `fraud.%`) mặc định bị ẩn. |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | Khóa ghi client. Chỉ Admin CMS (P5.1) cập nhật config.                                                                                      |
| **15. `public.orders`**<br>_(Khung nạp tiền)_                 |     **SELECT**      |           ❌            |         ⚠️ _(Chính chủ)_         |                ❌                 |                  ✅                  | Đơn hàng nạp tiền chỉ chính chủ xem được (`auth.uid() = user_id`).                                                                          |
|                                                               | **INSERT / U / D**  |           ❌            |                ❌                |                ❌                 |                  ✅                  | **Chưa kích hoạt** (Khóa ghi client 100%). Sẽ kích hoạt cho Webhook xử lý thanh toán ở Phase P6.x+.                                         |

---

## 3. TÓM TẮT CÁC QUY TẮC BẢO MẬT CỐT LÕI (CORE SECURITY INVARIANTS)

1. **Khóa ghi mặc định toàn hệ thống (`Deny-write by default`)**:
   - 14/15 bảng cơ sở dữ liệu cấm 100% mọi quyền ghi (`INSERT`, `UPDATE`, `DELETE`) từ client thông thường.
2. **Policy ghi DUY NHẤT mở cho client (`public.user_equipped`)**:
   - Chỉ cho phép client tự đổi trang bị khi và chỉ khi thỏa mãn ràng buộc `WITH CHECK (EXISTS (SELECT 1 FROM user_inventory WHERE user_id = auth.uid() AND item_id = user_equipped.item_id))`.
3. **Chống Leo Thang Đặc Quyền Trên `public.profiles`**:
   - Trigger `protect_profile_system_fields` chặn đứng mọi nỗ lực sửa đổi `role`, `user_id`, và `is_anonymous` từ phía client.
4. **Sổ cái Bất Biến (Append-Only Invariant)**:
   - 3 bảng chứng từ `wallet_transactions`, `purchases`, `audit_logs` được khóa cứng bằng Trigger `BEFORE UPDATE OR DELETE -> RAISE EXCEPTION`.
