# Nghiệm thu Phase P0.2a: tsconfig riêng cho packages/engines

- **Mã phase**: `P0.2a`
- **Mục tiêu**: Ràng buộc "engine thuần" được enforce ở TẦNG TYPECHECK (TypeScript compiler), không chỉ ở lint.
- **Trạng thái**: ✅ **HOÀN THÀNH 100% (DoD Approved)**

---

## 1. Các thành phần đã triển khai

### A. Cấu trúc TypeScript Multi-Project & Solution Root

- [`tsconfig.base.json`](file:///e:/Project/PlayFusion/tsconfig.base.json): Chứa các compiler options nền tảng strict: `strict: true`, `noUncheckedIndexedAccess: true`, `verbatimModuleSyntax: true`, `skipLibCheck: true`, `moduleResolution: "bundler"`.
- [`packages/engines/tsconfig.json`](file:///e:/Project/PlayFusion/packages/engines/tsconfig.json):
  - `lib: ["ES2022"]` — **TUYỆT ĐỐI KHÔNG CÓ DOM, DOM.Iterable**.
  - `types: []` — Không tự động nạp type browser từ node_modules.
  - Không cấu hình `jsx`.
  - `noEmit: true`.
- [`tsconfig.app.json`](file:///e:/Project/PlayFusion/tsconfig.app.json):
  - Kế thừa `tsconfig.base.json`.
  - `lib: ["ES2020", "DOM", "DOM.Iterable"]`, `jsx: "react-jsx"`.
  - `include: ["src"]`, path aliases `@/*` và `@engines/*`.
- [`tsconfig.node.json`](file:///e:/Project/PlayFusion/tsconfig.node.json): Cho `vite.config.ts`, `tailwind.config.ts`.
- [`tsconfig.json`](file:///e:/Project/PlayFusion/tsconfig.json): Solution root gom cả 3 project references, cho phép `tsc -b` kiểm tra toàn bộ hệ thống bằng 1 lệnh duy nhất.
- [`package.json`](file:///e:/Project/PlayFusion/package.json): Cập nhật `"typecheck": "tsc -b"`.

### B. Cập nhật Tài liệu Quy Tắc Vàng

- [`packages/engines/README.md`](file:///e:/Project/PlayFusion/packages/engines/README.md): 5 quy tắc vàng bất biến (Không DOM, không React/UI, không import từ src/, deterministic 100%, độc lập trên 3 môi trường: Client View, Worker AI, Supabase Edge/Deno).

---

## 2. Bảng đối chiếu Definition of Done (DoD)

| Tiêu chí DoD                         | Kết quả kiểm chứng                                                                               | Trạng thái |
| :----------------------------------- | :----------------------------------------------------------------------------------------------- | :--------: |
| **Chặn `window` ở tầng Typecheck**   | Thêm `window.location.href` $\rightarrow$ `tsc -b` báo `error TS2304: Cannot find name 'window'` |   ✅ ĐẠT   |
| **Chặn `document` ở tầng Typecheck** | Thêm `document.title` $\rightarrow$ `tsc -b` báo `error TS2584: Cannot find name 'document'`     |   ✅ ĐẠT   |
| **Kiểm tra 2 vùng bằng 1 lệnh**      | `npm run typecheck` (`tsc -b`) kiểm tra cả `src/` (có DOM) và `packages/engines/` (không DOM)    |   ✅ ĐẠT   |
| **Codebase sạch 100%**               | `typecheck`, `lint`, `format:check`, `test`, `build` đều đạt exit code 0                         |   ✅ ĐẠT   |

---

## 3. Bằng chứng thực nghiệm DoD

### A. Output khi cố ý sử dụng `window` trong `packages/engines/_smoke.ts`:

```text
> playfusion@0.1.0 typecheck
> tsc -b

packages/engines/_smoke.ts(10,18): error TS2304: Cannot find name 'window'.
```

### B. Output khi cố ý sử dụng `document` trong `packages/engines/_smoke.ts`:

```text
> playfusion@0.1.0 typecheck
> tsc -b

packages/engines/_smoke.ts(10,25): error TS2584: Cannot find name 'document'. Do you need to change your target library? Try changing the 'lib' compiler option to include 'dom'.
```

### C. Output khi mã nguồn tuân thủ kiến trúc chuẩn:

```text
> playfusion@0.1.0 format:check
> prettier --check .
All matched files use Prettier code style!

> playfusion@0.1.0 lint
> eslint .

> playfusion@0.1.0 typecheck
> tsc -b

> playfusion@0.1.0 test
> vitest run
 ✓ packages/engines/_smoke.test.ts (5 tests) 4ms
 Test Files  1 passed (1)
      Tests  5 passed (5)

> playfusion@0.1.0 build
> tsc -b && vite build
✓ 32 modules transformed.
✓ built in 1.59s
```
