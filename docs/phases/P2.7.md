# TÀI LIỆU NGHIỆM THU PHASE P2.7: VẬN HÀNH FREE TIER, SAO LƯU & AN NINH DATABASE

**Ngày hoàn thành**: 19/08/2026  
**Phiên bản**: v0.5.0  
**Mã Commits**: `53134e7`, `519e6a2`  
**Trạng thái nghiệm thu**: ✅ **NGHIỆM THU (APPROVED)**

---

## 1. TÓM TẮT (5 DÒNG)

- **Mục tiêu**: Xây dựng hệ thống tự động giữ nhịp tim chống tạm dừng (Anti-Pause Keepalive) cho 2 project Supabase dev/prod, thiết lập pipeline sao lưu cơ sở dữ liệu định kỳ có mã hóa AES-256 và chính sách lưu trữ retention 8 tuần, diễn tập thành công quy trình khôi phục cục bộ (Local Restore Runbook), và đóng gói kiểm toán an ninh RLS tự động.
- **Kết quả**: 3 workflow GitHub Actions vận hành trơn tru, script giải mã cục bộ bảo mật an toàn, runbook phục hồi thảm họa đầy đủ bảng đối chiếu 15 bảng và smoke test nghiệp vụ, 100% test pass.
- **Trạng thái**: ✅ **ĐÃ HOÀN TẤT THI CÔNG & NGHIỆM THU TOÀN DIỆN (APPROVED)**.

---

## 2. NHỮNG GÌ ĐÃ TẠO / SỬA

| File / Module                                                                                      | Vai trò                        | Ghi chú sử dụng                                                                                                                                                                               |
| :------------------------------------------------------------------------------------------------- | :----------------------------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`.github/workflows/keepalive.yml`](file:///e:/Project/PlayFusion/.github/workflows/keepalive.yml) | Workflow Giữ Nhịp Tim          | Chạy mỗi 3 ngày (`17 21 */3 * *` UTC), query trực tiếp bảng `public.games` với `service_role` key, retry 3 lần, self-touch chống GitHub tắt sau 60 ngày.                                      |
| [`.github/workflows/backup.yml`](file:///e:/Project/PlayFusion/.github/workflows/backup.yml)       | Workflow Sao Lưu & Mã Hóa      | Chạy Chủ Nhật hàng tuần (`23 20 * * 0` UTC), `pg_dump` Session Pooler $\rightarrow$ mã hóa OpenSSL AES-256 $\rightarrow$ đẩy lên repo backup riêng qua SSH Key $\rightarrow$ Retention 8 bản. |
| [`.github/workflows/rls-audit.yml`](file:///e:/Project/PlayFusion/.github/workflows/rls-audit.yml) | Workflow Kiểm Toán An Ninh RLS | Chạy Chủ Nhật hàng tuần (`23 21 * * 0` UTC), chạy `npm run test:rls` tự động trên project DEV, tự dọn dẹp user test sạch sẽ.                                                                  |
| [`scripts/decrypt-backup.sh`](file:///e:/Project/PlayFusion/scripts/decrypt-backup.sh)             | Script Giải Mã Cục Bộ          | Nhận file `.dump.enc`, đọc passphrase an toàn từ stdin hoặc env (chống lộ bash history), xuất ra file `.dump` và kiểm tra mục lục với `pg_restore`.                                           |
| [`docs/ops/restore-runbook.md`](file:///e:/Project/PlayFusion/docs/ops/restore-runbook.md)         | Runbook Khôi Phục & Thảm Họa   | Hướng dẫn diễn tập Docker local, bảng đối chiếu 15 bảng và 5 tiêu chí số liệu, 3 smoke test queries, và quy trình Disaster Recovery 5 bước.                                                   |
| [`README.md`](file:///e:/Project/PlayFusion/README.md)                                             | Tài Liệu Hướng Dẫn             | Cập nhật mục "Vận hành & Bảo vệ Dữ liệu (Operations - P2.7)".                                                                                                                                 |

---

## 3. HỢP ĐỒNG CÔNG KHAI (PUBLIC CONTRACTS)

### A. 3 Workflows Vận Hành Tự Động

| Workflow      | File                              | Lịch chạy Cron (UTC)         | Mục đích vận hành                                                                  | Kênh cảnh báo lỗi                  |
| :------------ | :-------------------------------- | :--------------------------- | :--------------------------------------------------------------------------------- | :--------------------------------- |
| **Keepalive** | `.github/workflows/keepalive.yml` | `17 21 */3 * *` (Mỗi 3 ngày) | Gửi API activity giữ 2 project Dev/Prod không bị Supabase auto-pause sau 7 ngày.   | GitHub Actions Email khi exit != 0 |
| **DB Backup** | `.github/workflows/backup.yml`    | `23 20 * * 0` (Chủ Nhật)     | Sao lưu, nén, mã hóa AES-256 và lưu 8 tuần vào repo `webgamehub-backups`.          | GitHub Actions Email khi exit != 0 |
| **RLS Audit** | `.github/workflows/rls-audit.yml` | `23 21 * * 0` (Chủ Nhật)     | Chạy live test suite `test:rls` trên DEV để kiểm toán liên tục chính sách bảo mật. | GitHub Actions Email khi test fail |

### B. Danh Mục Toàn Bộ Repository Secrets

| Tên Secret                    | Sử dụng trong Workflow            | Mục đích & Nguồn lấy                                                                                |
| :---------------------------- | :-------------------------------- | :-------------------------------------------------------------------------------------------------- |
| `KEEPALIVE_SUPABASE_URL_DEV`  | `keepalive.yml`                   | Project Dev $\rightarrow$ Project Settings $\rightarrow$ API $\rightarrow$ Project URL              |
| `KEEPALIVE_SERVICE_ROLE_DEV`  | `keepalive.yml`                   | Project Dev $\rightarrow$ Project Settings $\rightarrow$ API $\rightarrow$ `service_role` (secret)  |
| `KEEPALIVE_SUPABASE_URL_PROD` | `keepalive.yml`                   | Project Prod $\rightarrow$ Project Settings $\rightarrow$ API $\rightarrow$ Project URL             |
| `KEEPALIVE_SERVICE_ROLE_PROD` | `keepalive.yml`                   | Project Prod $\rightarrow$ Project Settings $\rightarrow$ API $\rightarrow$ `service_role` (secret) |
| `BACKUP_DB_URL_PROD`          | `backup.yml`                      | Project Prod $\rightarrow$ Connect $\rightarrow$ URI $\rightarrow$ **Mode: Session (port 5432)**    |
| `BACKUP_DB_URL_DEV`           | `backup.yml`                      | Project Dev $\rightarrow$ Connect $\rightarrow$ URI $\rightarrow$ **Mode: Session (port 5432)**     |
| `BACKUP_ENCRYPTION_KEY`       | `backup.yml`, `decrypt-backup.sh` | Passphrase mã hóa AES-256 ngẫu nhiên $\ge 32$ ký tự (Lưu trong Password Manager).                   |
| `BACKUP_DEPLOY_KEY`           | `backup.yml`                      | SSH Private Key (ED25519) cấp quyền ghi vào repo `webgamehub-backups`.                              |
| `RLS_SUPABASE_URL_DEV`        | `rls-audit.yml`                   | Project Dev URL                                                                                     |
| `RLS_ANON_KEY_DEV`            | `rls-audit.yml`                   | Project Dev Anon Public Key                                                                         |
| `RLS_SERVICE_ROLE_DEV`        | `rls-audit.yml`                   | Project Dev Service Role Key                                                                        |

### C. Lịch Công Việc Thủ Công Định Kỳ (Manual Operations Routine)

1. **Hàng tháng (Monthly Check)**: Mở tab **Actions** trên GitHub kiểm tra lịch chạy (đảm bảo không bị GitHub tự ngắt sau 60 ngày repo không có commit).
2. **Hàng quý (Quarterly Dry-Run)**: Tải bản backup mới nhất, chạy diễn tập restore theo [`docs/ops/restore-runbook.md`](file:///e:/Project/PlayFusion/docs/ops/restore-runbook.md) và cập nhật Biên bản Diễn tập.

---

## 4. QUYẾT ĐỊNH THIẾT KẾ TRONG PHASE

| Quyết định                               | Lý do                                                                                                         | Phương án đã loại                                                                                 |
| :--------------------------------------- | :------------------------------------------------------------------------------------------------------------ | :------------------------------------------------------------------------------------------------ |
| **Ping qua PostgREST Query Thật**        | Đảm bảo 100% sinh ra database transaction và API request được tính vào chỉ số active trên Supabase Dashboard. | Chỉ ping endpoint `/auth/v1/health` $\rightarrow$ Không chắc chắn được tính là database activity. |
| **Lưu Backup trên Private GitHub Repo**  | Hoàn toàn miễn phí, không cần đăng ký thẻ thanh toán, bảo mật qua Deploy Key riêng biệt.                      | Cloudflare R2 $\rightarrow$ Yêu cầu thẻ tín dụng; AWS S3 $\rightarrow$ Phức tạp cấu hình IAM.     |
| **Mã hóa OpenSSL AES-256-CBC**           | Có sẵn 100% trên runner và máy phát triển Linux/macOS/Windows, không cần cài đặt thêm tool ngoài.             | `age` encryption tool $\rightarrow$ Phải cài thêm binary trên runner.                             |
| **Tách `test:rls` thành Workflow Riêng** | Giữ cho CI push code hàng ngày hoàn toàn chạy offline, độc lập mạng và cực nhanh ($< 30\text{s}$).            | Ép `test:rls` vào mỗi commit $\rightarrow$ Làm chậm CI và phụ thuộc kết nối mạng dev.             |

---

## 5. NHỮNG GÌ CHƯA LÀM / NỢ KỸ THUẬT

| Hạng mục hoãn lại                           | Lý do                                                                                                                                    |   Mã phase dự kiến xử lý    |
| :------------------------------------------ | :--------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------: |
| **Sao lưu `auth.users` qua Management API** | Supabase managed DB không cấp quyền superuser cho `pg_dump` schema `auth`. Người dùng Google đăng nhập lại sẽ tự map lại profile/wallet. |  Backlog / Giai đoạn Scale  |
| **Tự động hóa Diễn tập Restore trên CI**    | Cần runner cấu hình Docker-in-Docker và nạp test container $\rightarrow$ Chưa cần thiết ở quy mô hiện tại.                               |  `P5.2` (DevOps Hardening)  |
| **Supabase CLI Local Environment**          | Cần cài đặt Docker và cấu hình đồng bộ migration tự động trước khi bước vào Multiplayer Realtime / Edge Functions.                       | **TRƯỚC `P3.2` (Hạn chót)** |

---

## 6. HƯỚNG DẪN VẬN HÀNH & DIỄN TẬP

- **Kích hoạt Giữ Nhịp Tim Thủ Công**: Vào tab **Actions** $\rightarrow$ `Supabase Anti-Pause Keepalive` $\rightarrow$ Bấm **Run workflow**.
- **Kích hoạt Sao Lưu Thủ Công**: Vào tab **Actions** $\rightarrow$ `Supabase Database Backup & Encryption` $\rightarrow$ Chọn môi trường (`prod`/`dev`/`both`) $\rightarrow$ Bấm **Run workflow**.
- **Kích hoạt Kiểm Toán RLS Thủ Công**: Vào tab **Actions** $\rightarrow$ `RLS Security Audit` $\rightarrow$ Bấm **Run workflow**.
- **Quy trình Diễn tập Restore Cục bộ**: Thực hiện theo hướng dẫn chi tiết tại [`docs/ops/restore-runbook.md`](file:///e:/Project/PlayFusion/docs/ops/restore-runbook.md).

---

## 7. ĐẦU VÀO GỢI Ý CHO PHASE KẾ TIẾP [P3.1: TẦNG KẾT NỐI REALTIME TRANSPORT]

1. **Khép lại toàn diện Nhóm 2**: Toàn bộ hạ tầng dữ liệu, phân quyền RLS, đồng bộ offline outbox, hồ sơ người chơi, và vận hành chống pause / sao lưu dữ liệu đã hoàn tất vững chắc 100%.
2. **Nhiệm vụ trọng tâm Phase P3.1**:
   - Mở thư mục [`src/transport/`](file:///e:/Project/PlayFusion/src/transport) với module kết nối **Supabase Realtime** (Broadcast / Presence / Postgres Changes).
   - **Kỷ luật Free Tier**: Chỉ kết nối WebSocket khi đang trong phòng đấu/ván chơi online, tự động ngắt kết nối (disconnect) ngay khi rời phòng để không vượt quá trần 200 kết nối đồng thời của Supabase Free Tier.

---

> ### 🏆 DÒNG KHÉP SỔ TOÀN DIỆN NHÓM 2 (P2.1 → P2.7):
>
> **"NHÓM 2 (P2.1 → P2.7) HOÀN TẤT — tài khoản, 15 bảng, RLS có test máy, repositories, đồng bộ offline-first, hồ sơ thống kê, vận hành free tier có backup đã diễn tập."**
